<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.arch.application.form.mapper.FormMapper">

    <!--ALTER TABLE ${tableName} ADD ${fieldName} VARCHAR(100) ;-->
    <select id="alertForm" parameterType="Form" >
        ALTER TABLE ${formName} ADD ${formTableName} VARCHAR(100) DEFAULT NULL COMMENT #{formDesc};
    </select>
    <!--DEFAULT NULL COMMENT #{fieldDesc};-->

    <insert id="insert" parameterType="Form" useGeneratedKeys="true" keyProperty="formId" keyColumn="form_id">
        insert into upload_form (form_id, form_name, form_desc, create_time)
        values ( 0, #{formName}, #{formDesc}, now())
    </insert>

    <select id="queryAll" resultType="Form">
        select * from upload_form order by create_time desc
    </select>

    <select id="queryOne" resultType="Form">
        select * from upload_form where form_id = #{formId}
    </select>

    <delete id="deleteById">
        delete from upload_form where form_id = #{formId}
    </delete>

    <update id="update" parameterType="Form">
        update upload_form a set a.form_name = #{formName}, a.form_desc = #{formDesc} where a.form_id = #{formId}
    </update>

    <update id="updateFormTableName" parameterType="Form">
        update upload_form a
        set a.form_table_name = #{formTableName}
        where a.form_id = #{formId}
    </update>

    <select id="queryFormAndRows" resultType="FormAndRows">
        select * from upload_form
        <where>
            <if test="searchValue != '' and searchValue != null ">
                form_name like '%' || #{searchValue} || '%'
                or
                form_desc like '%' || #{searchValue} || '%'
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="countByFormName" resultType="Integer">
        select count(1) from upload_form where form_name = #{formName}
    </select>
</mapper>
