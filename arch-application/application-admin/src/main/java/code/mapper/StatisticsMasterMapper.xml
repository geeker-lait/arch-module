<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="code.mapper.StatisticsMasterMapper">
    <sql id="tableName">
        statistics_master
    </sql>

    <sql id="baseColumn">
        id,ip,source,phone_no,resource_id,resource_typ,behavior_typ,device_id,device_name,os_typ,os_version,mac,app_id,account_id,cost,st
    </sql>

</mapper>